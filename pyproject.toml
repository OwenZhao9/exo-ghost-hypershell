[project]
name = "exo-ghost"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "pyserial>=3.5",
    "websockets>=17.1",
]
