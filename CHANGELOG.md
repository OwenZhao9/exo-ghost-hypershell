# 更新日志

本日志供后续修改代码时追溯变更、受影响文件与验证结论。每次功能变更都同步更新本日志、`docs/FEATURES.md` 和本地展示网页 `site/`。

## 2026-09-23

### 眼镜识别后接入 jEV 判断

- `product_features/guide/vision.py` 保留 EvoMap 的严格格式与置信度检查并返回识别置信度；`product_features/guide/decision.py` 将已校验的方向、画面事实和物体标签送入现有 `jev-decide` TypeSafe 后端，不发送 JPEG。仅当 jEV 实际返回同向、高置信、未降级的结果时才形成最终左/右结论；后端缺密钥、超时、降级或两次意见不一致均归为 unknown。`demo.py` 在同一个后台工作线程顺序运行识别与复核，继续与下一张拍照并行，分别持久化原识别和复核状态。
- 产品照片卡片显示识别方向及 jEV 复核状态；语音和电机支架演示再次核对后端、双侧结论和置信度才接受左/右。兼容性：原 EvoMap Gateway Key 不能用作 TypeSafe Key，需另配 `TYPESAFE_API_KEY`。当前本机未配置该密钥，真实 jEV 调用和眼镜到电机的现场动作均未验收。验证：`uv run pytest tests/ -q` 157 项、`node --test tests/product-device.test.mjs tests/product-guide-voice.test.mjs` 7 项通过，覆盖同向、冲突、低置信、回退和无密钥时不产生方向。`site/` 已在 Chrome 检查桌面内容，并在 390px 视口检查眼镜卡片；一并修正手机端装饰字符横向溢出。

### 眼镜连续拍照演示分支

- 根据用户对评委支架 Demo 的要求，加入可选的“视觉结果 → 单次抬腿”链路：右侧结果抬左腿，左侧结果抬右腿；默认关闭，控制服务和产品服务均需显式开启，页面还需用户手动开启本轮抬腿演示。`control/guide_cue.py` 固定 20° 相对目标、0.5 Nm 上限、10°/s 目标角速度、1 Nm/s 斜坡和 4 秒到期归零，目标角还需留在左髋 ≥−80° / 右髋 ≤+80°的软边界内；`runtime/commands.py` 限定桌面档、zero 策略、静止且新鲜的关节数据，`runtime/service.py` 禁止与保活或自动策略并用，本机 WebSocket 仅监听回环地址。页面只对新鲜且高置信左/右照片发送请求，无法判断和过期结果不动作，并等待设备状态确认；停止时归零。兼容性：原默认只读运行方式不变，真机控制服务必须切到包含新命令的本分支，且同一时间只能有一个进程占用串口。验证：`uv run pytest tests/ -q` 149 项、前端测试 6 项通过；真机动作仍待现场验收，不能用于穿戴或真实引路。
- 按用户更正将四条固定提示音离线合成并接入本机眼镜 Demo：识别结果为左、右或无法判断时播放对应 WAV；手动停止或本轮自然结束时播放停止提示。开始演示后自动启用电脑声音，刷新后可手动“开启语音并试听”；不补播历史照片，播放失败会明确显示。`product_features/guide/voice.js` 串行播放、停止时中断旧提示；`product/server.py` 允许只读提供 `.wav` 静态资源。音频来源、许可、对应文字与验证记录见 `product_features/guide/audio/README.md`。与外骨骼控制链路和照片上传条件无关，不调用系统语音合成。验证：四条音频经中文语音识别复核、WAV 结构及本机 HTTP 读取检查；`uv run pytest tests/ -q` 145 项、前端测试 5 项通过。Chrome 试听、新识别的“无法判断”和手动停止提示均收到播放结束事件，390px 产品视口与本地展示页桌面/手机布局已检查。真机拍照曾出现一次蓝牙/设备错误，随后重试连续采集 6 张并手动停止；停止后未上传排队照片。
- 采图和 EvoMap 识别改为独立后台线程：采完一张即排队识别，同时继续拍下一张；单个识别线程按拍摄顺序处理，不会把结果配到其他照片。页面将新照片和照片库新项插到最上方，保留旧卡片；完成识别后在对应照片叠加方向状态和经过坐标校验的可见物体框，不修改原始 JPEG。排队未发出的照片在手动停止后标记中断；已经发出的识别请求可完成。私有照片、识别元数据仍只留本机，不改变外骨骼控制链路或上传授权边界。验证：E06-0194 真机第 2 张在第 1 张识别完成前约 8 秒开始拍摄，第 1 张返回“手臂”“黄色标牌”位置标注；Chrome 页面确认第 6 张置顶、对应照片标注，390px 产品视口无横向溢出。`uv run pytest tests/ -q` 144 项、设备边界 JS 测试 3 项通过；本地展示页在 Chrome 桌面和手机视口检查。
- 按用户反馈将单张替换式预览改为纵向追加的拍照记录：每张 JPEG 有独立卡片、拍摄时间、识别状态与文字判断，新一张出现时保留旧卡片和旧结论。照片库也按时间向下追加，不再在轮询时重建列表。演示记录的元数据与判断保存在本分支本机 SQLite 产品数据库，服务重启可恢复；照片仍在私有眼镜目录，页面通过按文件名校验的本机接口读取。兼容现有手动选图描述入口，不改拍照次数上限、外骨骼控制或 EvoMap 上传边界。
- 列表版现场验证：E06-0194 连续产生三张新照片，Chrome 中第 2、3 张分别追加在前一张下方；第一张的画面和结果保持可见，停止后第三张标记为“识别未完成”。刷新页面仍显示三张卡片及各自状态；390px 手机视口按单列排列。`uv run pytest tests/ -q` 142 项和设备边界 JS 测试 3 项通过。旧版本留下的照片仍可在照片库查看，但当时未保存的自动识别文字无法恢复。
- `feat/glasses-demo-GPT` 增加可选的本机眼镜 Demo：产品服务需显式指定 Luma 拍照程序、`E06-0194` 广播名和 EvoMap 自动识别开关；页面手动开始或停止，后台顺序拍照、检查 JPEG、自动识别并显示最近一张。每轮最多 10 张，连续失败 3 次自动停止；停止时终止正在运行的拍照子进程。与外骨骼控制链路隔离，不发送力矩命令。
- EvoMap Demo 结果只接受严格校验的 JSON；低置信度、模糊描述或返回格式错误都显示“无法判断”。方向是画面左右较空侧的演示比较，不是可靠通路判断。真人录音尚未提供，页面明确显示语音不可用，不调用设备、浏览器或操作系统的语音合成。
- 修正连续拍照时上一张结果过早消失的问题：下一张拍摄期间保留并标记“最近照片”结论，新照片产生时再清除旧结论，避免把旧判断配到新画面。停止期间仍能查看上一次完成的结果。
- 本地展示页的产品代码链接改为本演示分支，避免从演示功能说明跳到尚未包含该代码的集成分支。
- 2026-09-23 现场验证：E06-0194 连续采集两张 368×480 JPEG，耗时 11.5 秒和 11.1 秒；第一张 EvoMap 识别耗时 10.6 秒，桌面暗光画面返回“无法判断”。调整页面布局和 JSON 解析后，再采集一张耗时 11 秒、识别耗时 8.7 秒，Chrome 页面显示模型给出的暗光与模糊原因，手动停止生效。本地展示页桌面和 390px 手机视口已检查。验证 `uv run pytest tests/ -q` 142 项通过，前端设备边界测试 3 项通过。私人照片、网关密钥和识别结果未加入 Git 或本地展示页。

### 眼镜照片描述试验

- 2026-09-23 当前眼镜确认为 `E06-0194`（早先文档记录的是 `E06-00C8`）。独立眼镜项目的纯拍照命令按当前广播名锁定设备，并重新编译；Mac 上完成 BLE 扫描、握手（电量 93%）与拍照，保存 368×480 JPEG。Chrome 产品页刷新后列出新照片，手动请求 EvoMap 得到完整中文描述。照片只保存在本机私有目录，未进入 Git 或 `site/`。网页仍不自动拍照，也不提供连续视觉或实际引路。验证：Rust release 示例编译、拍照命令退出码 0、JPEG 文件检查、Chrome 页面结果。

- 找回本机 `/Users/owenzhao/eyeGalss/luma-core` 的 Luma 眼镜采图代码；保持原工作区未提交改动不变。`feat/glasses-guide-GPT` 新增只读照片列表、手动选图和 EvoMap Gateway 视觉请求封装，合入产品工作区。Gateway Key 从 `EVOMAP_GATEWAY_API_KEY` 读取，不进入前端、日志或 Git。旧照片标识历史，不推断道路可通行，不向外骨骼发送命令。
- 产品启动可指定 `--glasses-dir`；真实图像发送需人手点击。已在 EvoMap 创建仅绑定 Gemini 3.1 Pro、30 天到期的 Gateway Key，保存于 Git 忽略的本机 `data/product/evomap_gateway.key`（0600）；产品服务启动时读入环境变量。先用用户允许的历史眼镜 JPEG 验证图片透传和中文描述。初次模型服务繁忙返回 429；较低的回复上限又导致半句输出，因此改用低思考等级与 2048 token 上限，复验得到完整描述。`site/` 不展示私人照片。眼镜音频和实际引路未实现。

### 改为本地展示

- 用户最新要求停止公网发布，Cloudflare Pages 项目 `exo-ghost` 已删除。`site/` 保留为本地网页，运行方法见 `site/README.md`。
- 项目规则与全局跨会话规则改为：每次功能更新同步 `site/`、`docs/FEATURES.md`、`CHANGELOG.md`，只在本机验证，不部署公网。

### 产品工作区首版与展示说明同步

- 按用户最新要求改为 DJI Fly 官网风格的浅色主题：白底、黑灰信息层级、蓝色操作重点，并在总览加入已获准使用的外骨骼正面图。`site/` 产品示意图同步浅色外观；运动页双侧曲线改为蓝/橙并同步文字与辅助说明。仅改变视觉与文案，不改变真机就绪判定、控制命令或历史计算。验证：Chrome 桌面/手机视口、本地展示页、前端设备边界测试。
- 产品界面视觉更新：根据 Oura 的当日重点、Garmin 的数据层级与 DJI Fly 的设备状态可见性，重做 `dashboard/product/` 的深色工作区、总览信息层级、导航和运动曲线区域；`product_features/modes/view.js` 仅调整结构与曲线配色。设备来源未知仍明确显示，缺少新帧时不绘制示例曲线；原控制边界保持不变。
- 本地 `site/` 中的产品示意图改为对应的新界面结构，移除原示意图中没有实际数据支撑的柱形波动和频率数字。设计来源与取舍记录在 `docs/product-ui-reference.md`。验证：Chrome 桌面及 390px 手机视口检查产品五个页面与本地展示页；JS 语法和现有前端设备边界测试。

- `integration/product-GPT` 的 `9e7996a` 集成运动模式、行走档案、个人记忆和成长徽章；7 个新分支各有独立 worktree，产品端口自动区分，SQLite 数据库默认在各自工作目录。
- 新增产品页面及模块接口。曲线订阅现有控制服务，默认只读、不占用串口；开启产品控制仍需确认实时真机来源及就绪状态。个人偏好保存、版本记录和徽章解锁不会触发设备施力。
- 档案收录从 CSV 与配套 JSONL 确认来源、按内容去重，跳过无效帧及断流间隔；仿真记录不进入产品档案。四枚成长徽章只统计完整真实穿戴运动，排除桌面、急停和静止记录。
- 兼容性：集成版控制状态新增 `body`、`profile`；旧服务仍可显示曲线，但缺少真机来源标记时新产品页面不开放控制。切换控制服务必须先释放串口。原实时控制安全阈值与斜坡未改。
- 将上述行为、数据来源、验证范围和未完成项补入 `docs/FEATURES.md`。展示网页同步四个已实现模块和产品分支代码入口；引路、肌力测量、疲劳/省力结论不作为现成功能宣传。网页不包含私人档案或本机控制入口。
- 验证：134 项 Python 测试、3 项前端控制测试通过；Chrome 检查真机数据只读显示、档案收录及重启保留、桌面记录不计徽章。本地展示页的桌面/移动显示见本次交付记录。

### 展示网页与同步规则

- 新增静态展示网页 `site/`：展示 Ghost 控制链路、三层 Agent、Sim to Real、3D 模型与产品集成分支中的运动模式、行走档案、个人记忆、成长徽章。Cloudflare 曾测试发布，现已撤下。
- 3D 展示使用已确认可用的 Hypershell 官方素材、Tripo 生成模型及真实设备桌面标定回放。网页与本机实时控制服务隔离；无施力入口。
- 新增项目规则 `AGENTS.md`、代码功能索引 `docs/FEATURES.md` 和站点维护说明 `site/README.md`。`.gitignore` 不再忽略项目规则，便于后续工作区合并并由其他会话读取。
- 首次网页检查发现模型加载后预览图未隐藏，已修正；测试发布时内嵌贴图的 `blob:` 地址曾被云端策略拦截，撤站前已修复。Wrangler 缓存目录加入忽略列表。
- 验证：本机静态网页的桌面、移动端显示和 3D 回放。

### 已有功能的分支来源

- `feat/auto-reconnect-GPT`：串口恢复与本机 3D 真机/仿真/回放视图。展示网页复制经确认的模型和桌面回放素材，未改动该分支控制逻辑。
- `integration/product-GPT`：本机产品工作区，含运动模式、行走档案、个人记忆、成长徽章。展示网页仅描述该分支已经实现的范围；主分支尚未合入该产品代码。
