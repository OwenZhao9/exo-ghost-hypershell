# 更新日志

本日志供后续修改代码时追溯变更、受影响文件与验证结论。每次功能变更都同步更新本日志、`docs/FEATURES.md` 和本地展示网页 `site/`。

## 2026-09-23

### iPhone 8 移动端控制分支

- 用户确认 iPhone 与 Mac 已可正常通信。`1.0 (4)` 手机界面增加左右髋角速度读数与最近 10 秒曲线、腰部姿态和加速度模长，并分别标记控制服务和腿部数据是否实时。腿板掉线、串口恢复、数据超时会清除旧腿部曲线；腰部读数独立过期清空。未修改后端配对、力矩、斜坡或控制命令。兼容性：旧构建仍可连接同一 Mac 服务。验证：iOS 16 模拟器目标编译与发行归档成功，模拟器检查离线状态与曲线占位；TestFlight 构建 4 已上传、处理完成、加入仅 1 人的内部群组，App Store Connect 显示 iPhone 8 Plus 已安装；真机曲线和失联显示待现场复核。
- 按演示需求取消 App 扫码入口：`1.0 (3)` 在本机发行构建时从 Git 忽略的 `DemoPairing.local.xcconfig` 注入当前 Mac 局域网地址和口令；首次启动把口令存入 iPhone Keychain，并自动尝试连接。回到前台后自动重连；手动连接设置仍可修改。预置地址随 Wi-Fi/IP 改变会失效，需要重新构建或手动修改。配置文件仅本机 0600 权限且不入 Git，但发行 IPA 内可提取口令，故仅分发给已授权的内部测试者。
- `1.0 (3)` 已签名上传，App Store Connect 处理完成，测试说明保存并分配到只有 1 人的 `Ghost iPhone 内测` 群组。模拟器显示预置地址并自动发起连接；服务未启动时所有运动控制按钮保持禁用。完整 Python 测试 134 项通过；手机真机连接尚待确认。
- App Store Connect 随后显示内测账户已在一台 iPhone 8 Plus 安装 `1.0 (3)`；这仅确认分发与安装，不代表真机连接或控制已验证。
- 演示配对改为扫码：`tools/mobile_pairing.py --qr` 用当前 Mac 局域网地址与本机口令生成仅本机可读的二维码；iOS `PairingScanner.swift` 经相机扫描、格式与局域网地址校验后把口令存入 Keychain、地址存入本机设置，并自动尝试连接。保留手动输入；Mac 换网后可重新生成、重扫。二维码和口令均排除 Git，未把口令写入 App 包。iOS 构建号升至 `1.0 (2)`，增加相机用途说明。
- `1.0 (2)` 已由 App Store Connect 处理完成，测试说明已保存，并分配给只有账户持有人的 `Ghost iPhone 内测` 内部群组；用户设备上的邀请接受、安装和扫码仍待确认。
- 验证：扫码载荷与二维码文件权限测试通过；Mac Vision 解码确认二维码包含本机地址和对应口令；iOS 模拟器检查扫码入口和拒绝相机权限时的提示，iPhone 8 真实扫码未验证。当前运行的旧控制服务未重启，仍未载入手机配对后端。
- 新建 `feat/ios-mobile-GPT`，从串口自动重连分支独立开发。新增 `ios/GhostMobile/` SwiftUI 工程，最低 iOS 16；显示真实双侧髋角度、连接与安全状态，提供受限阻尼、轻助力、松劲和急停。
- `tools/webhub.py` 对非本机 WebSocket 加入随机口令配对与远端命令白名单；`runtime/mobile.py` 负责本机私有口令和两秒租约；`runtime/service.py` 在手机失联、租约过期、腿板异常或串口中断时清除手机控制状态。`tools/mobile_pairing.py` 只在 Mac 本机显示地址与口令。
- 兼容性：本机旧网页仍可操作；局域网客户端现在必须先配对。正在运行的旧进程不会自动载入新版后端，切换版本必须先释放唯一真机串口。App 只能在同网可信 Wi-Fi 连接 Mac；手机不直接连接设备串口。
- 验证：完整 Python 回归 132 项通过；Xcode 26.4 iOS 16 最低目标构建、发行签名归档和 IPA 导出成功。App Store Connect 已创建对应应用，TestFlight `1.0 (1)` 上传并处理完成，出口合规信息已填写，构建已分配给仅含账户持有人的内部测试群组，账号状态为“已邀请”。用户的 iPhone 8 安装、真实配对、控制及断线保护尚待现场验证。
- 同步本地展示网页说明 TestFlight 内测已开放，手机端仍在真机验证阶段。

### 串口持续恢复与本地说明同步

- `feat/auto-reconnect-GPT` 在每轮恢复时重新枚举串口，移除原有 40 次重试上限；服务可在设备未插入时启动仪表盘，随后持续等待真实设备。默认来源改为 `real`；仿真和自动回退需分别显式指定 `--body sim`、`--body auto`。
- 修复服务启动时无串口、稍后首次连上后未启动断流监测的问题（`a8d3653`）。新增回归测试覆盖首次连接之后再次断开并更换端口，恢复后的目标力矩为零。
- 串口断流后清除旧策略和保活脉冲，恢复时执行 15 秒零指令等待；仪表盘收到新帧即可恢复绘图，不把缺失值显示为正常零读数。正常关闭时允许接收 `DISABLE` 应答，避免误报关闭失败。
- 补齐 `docs/serial-recovery.md`、`docs/FEATURES.md` 与本地 `site/` 的行为说明，解释腿板全零推断和可选保活脉冲。保活会驱动左腿，防待机效果未验证；不作为已经解决自动关机宣传。
- 验证：126 项 Python 测试通过；桌面真机已检查实时数据、一次自然断流恢复和保活脉冲后归零。尚未完成反复物理拔插/重启压力测试、保活防待机效果及穿戴恢复验证。本次不重启运行中的硬件服务；启动监测修复在下次服务启动时载入。
- 本地网页已在 Chrome 检查桌面与 390 px 手机布局、模型加载、历史回放数值变化及暂停操作。静态网页只加载本地素材，不调用设备控制服务，未执行公网部署。

### 改为本地展示

- 用户最新要求停止公网发布，Cloudflare Pages 项目 `exo-ghost` 已删除。`site/` 保留为本地网页，运行方法见 `site/README.md`。
- 项目规则与全局跨会话规则改为：每次功能更新同步 `site/`、`docs/FEATURES.md`、`CHANGELOG.md`，只在本机验证，不部署公网。

### 产品工作区首版与展示说明同步

- `integration/product-GPT` 的 `9e7996a` 集成运动模式、行走档案、个人记忆和成长徽章；7 个新分支各有独立 worktree，产品端口自动区分，SQLite 数据库默认在各自工作目录。
- 新增产品页面及模块接口。曲线订阅现有控制服务，默认只读、不占用串口；开启产品控制仍需确认实时真机来源及就绪状态。个人偏好保存、版本记录和徽章解锁不会触发设备施力。
- 档案收录从 CSV 与配套 JSONL 确认来源、按内容去重，跳过无效帧及断流间隔；仿真记录不进入产品档案。四枚成长徽章只统计完整真实穿戴运动，排除桌面、急停和静止记录。
- 兼容性：集成版控制状态新增 `body`、`profile`；旧服务仍可显示曲线，但缺少真机来源标记时新产品页面不开放控制。切换控制服务必须先释放串口。原实时控制安全阈值与斜坡未改。
- 将上述行为、数据来源、验证范围和未完成项补入 `docs/FEATURES.md`。展示网页同步四个已实现模块和产品分支代码入口；引路、肌力测量、疲劳/省力结论不作为现成功能宣传。网页不包含私人档案或本机控制入口。
- 验证：134 项 Python 测试、3 项前端控制测试通过；Chrome 检查真机数据只读显示、档案收录及重启保留、桌面记录不计徽章。本地展示页的桌面/移动显示见本次交付记录。

### 展示网页与同步规则

- 新增静态展示网页 `site/`：展示 Ghost 控制链路、三层 Agent、Sim to Real、3D 模型与产品集成分支中的运动模式、行走档案、个人记忆、成长徽章。Cloudflare 曾测试发布，现已撤下。
- 3D 展示使用已确认可用的 Hypershell 官方素材、Tripo 生成模型及真实设备桌面标定回放。网页与本机实时控制服务隔离；无施力入口。
- 新增项目规则 `AGENTS.md`、代码功能索引 `docs/FEATURES.md` 和站点维护说明 `site/README.md`。`.gitignore` 不再忽略项目规则，便于后续工作区合并并由其他会话读取。
- 首次网页检查发现模型加载后预览图未隐藏，已修正；测试发布时内嵌贴图的 `blob:` 地址曾被云端策略拦截，撤站前已修复。Wrangler 缓存目录加入忽略列表。
- 验证：本机静态网页的桌面、移动端显示和 3D 回放。

### 已有功能的分支来源

- `feat/auto-reconnect-GPT`：串口恢复与本机 3D 真机/仿真/回放视图。展示网页复制经确认的模型和桌面回放素材，未改动该分支控制逻辑。
- `integration/product-GPT`：本机产品工作区，含运动模式、行走档案、个人记忆、成长徽章。展示网页仅描述该分支已经实现的范围；主分支尚未合入该产品代码。
