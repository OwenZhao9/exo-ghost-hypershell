# 更新日志

本日志供后续修改代码时追溯变更、受影响文件与验证结论。每次功能变更都同步更新本日志、`docs/FEATURES.md` 和本地展示网页 `site/`。

## 2026-09-23

### 修正 3D 左右腿通道

- 用户现场指出移动实体左腿时，3D 右腿在动。`site/app.js` 将人体模型旋转后的解剖左髋与外骨骼模型 `+Z` 组配对，并交换外骨骼网格对左右传感器的绑定；页面数值与真机控制通道不变。前后摆腿角度符号维持上一版设置。`node --check`、6 项站点测试和 `git diff --check` 通过；Chrome 桌面与 390px 手机、用户正在看的应用内标签页已加载新模型，应用内页在约 180 Hz 实时数据下重新对齐直立姿态。尚待用户再次单独摆动左腿确认现场左右结果。

### 修正 3D 前后迈腿方向

- 用户指出实体向前迈腿时 3D 模型向后摆。将 `site/data/twin-config.json` 的真机左右视觉角度符号同时反转；外骨骼与白膜人体使用同一映射。只改展示，不改电机控制方向、安全阈值或串口代码。JSON 校验及 6 项站点测试通过；Chrome 桌面和 390px 手机布局、历史记录播放已检查。真机目前处于重连状态，现场动作复核仍待设备恢复。

### 3D 页面新增动力辅助与健身阻力

- `site/` 新增两种手动运动模式入口，采用首次使用低强度预设：助力增益 0.1、阻力增益 0.3，均以 0.5 N·m 为上限。另设松劲和急停；当前模式从设备状态回报读取，页面加载和断线重连均不自动开启施力。
- `site/live.js` 跟踪状态和传感器帧新鲜度，`site/control.js` 只在真机来源、安全档、就绪状态、至少 50 Hz 和操作者放置状态确认齐全时放行模式命令。`runtime/service.py` 的状态广播补充实际 `body` 和 `profile`。旧控制进程缺少 `profile` 时按钮禁用，不影响实时 3D 观察。
- 更新项目规则、功能说明和本地网页维护说明。6 项站点测试和 122 项 Python 测试通过；Chrome 桌面与 390px 手机布局已检查，当前旧控制进程未报告 `profile` 时施力按钮保持禁用。现场对真机施力尚未验证；旧运行服务需要在确认设备状态后切换到新版，不能让两个控制进程同时占用串口。

### 3D 腰带与膝上固定环贴合

- `site/data/twin-config.json` 新增腰部和双侧腿环的部件分组、局部缩放及位移。`site/app.js` 在外骨骼双髋枢轴建好后收紧腰带及腿环，并将腿环中心从膝关节附近抬至上方约两指的视觉位置；腿环仍是各自髋部运动组的子节点，随历史回放或实时髋角一起转动。腿部支撑杆下段按高度平滑移动到新环位，解决最初只移动腿环时出现的连接缝。
- `site/app.js` 读取 `twin-config.json` 时禁用浏览器缓存；本地验证发现旧缓存缺少新 `fit` 字段会使 3D 视图启动失败，此更改后重新加载可正常启动。
- 同步本地展示文案、`docs/FEATURES.md` 与 `site/README.md`。`node --check site/app.js`、配置 JSON 校验、`git diff --check` 及 5 项站点测试通过；Chrome 桌面前后侧视、历史桌面记录与 390px 手机视口已检查，固定环随髋部姿态运动，支撑杆下端不再视觉脱开。这些是针对现有 Tripo 外骨骼和白膜人体的渲染参数；没有对真实绑带施力或执行人体尺寸测量。实际穿戴舒适度与松紧仍需用户现场确认。

### 白膜人体模型与穿戴朝向

- 以获准使用的 Hypershell 官方穿戴照片为人体比例与展示参考，制作正面无五官白膜参考图；Tripo image-to-model v3.1 生成连续人体网格，再用 Tripo biped/Mixamo 自动绑骨。生成与绑骨任务、消耗积分、参考图和来源见 `model-sources/README.md`，密钥没有写入仓库。
- `site/app.js` 将展示页从 Cesium 方块示例人体切换到新绑骨模型，去除贴图并渲染为半透明白膜。人物绕竖轴转 180° 修正与外骨骼的前后穿戴方向；相应交换左右髋骨映射，按髋骨位置对齐现有外骨骼，保留显隐切换与双髋实测角度联动。网页只读设备数据，不修改真机控制链路。旧示例人体文件已移除。
- 更新 `site/` 文案、模型来源说明及 `docs/FEATURES.md`。`node --test tests/site-human-model.test.mjs tests/site-live.test.mjs` 5 项通过，`node --check site/app.js` 与 `git diff --check` 通过；本地 Chrome 桌面和 390px 移动视口确认模型无五官、背部设备位置及页面显示，桌面实测记录可驱动左髋姿态。新模型是视觉近似，不提供膝、脚或全身动作测量；实机转向与穿戴步态仍需现场对照。

### 腰部转身与双侧摆腿跟随

- `site/live.js` 接收腰部陀螺仪数据和帧到达时间；`site/motion.js` 计算跨 ±180° 的短时 yaw 差，只在陀螺仪超过静止噪声门槛时更新相对朝向，长时间断流不补转。
- `site/app.js` 让外骨骼和半透明人体整体跟随相对转身，增加“转身跟随”开关；原有“对齐当前姿态”同时重设朝向。用户拖动视角仍可独立查看模型。该功能不发送设备指令，真实转身方向尚需现场动作对照。
- `site/motion.js` 根据实测双髋角度检测持续交替摆动；`site/index.html` 在实时数据区显示检测状态。腿部姿态仍由实测角度直接驱动，静止、单腿运动、断流和历史桌面记录均不标为实时交替摆腿。状态不等于脚步落地、穿戴确认或前进距离。
- 同步更新 `site/`、`docs/FEATURES.md` 与本日志。`node --test tests/site-live.test.mjs` 4 项通过；对历史桌面记录离线核查 636 帧，0 帧被标为交替摆腿。现有记录不含已验证的真实行走、膝/足或位置数据；转向方向和真人行走姿态尚需现场对照。

### 实体姿态跟随对齐

- `site/app.js` 保留实时 3D 姿态的首帧对齐基准，短暂断线重连时不再将新收到的位置当作零点。新增“对齐当前姿态”按钮，操作者可将当前桌面实体位置设为模型的视觉起点；不会发送任何设备命令。
- 用 `site/data/twin-replay.json` 的真实桌面记录核对左右髋数据到模型的联动：左髋从约 −36.2° 到 −25.4° 时，3D 外骨骼和人体腿部同步改变姿态。2026-09-23 当前真机流约 180 Hz；尚未进行手动摆动实体并与画面对照的现场验证。
- 同步更新本地展示页说明、`docs/FEATURES.md` 与 `site/README.md`。

### 本地 3D 视图订阅只读设备数据

- `site/live.js` 连接本机设备 WebSocket `ws://127.0.0.1:8765`，读取双髋角度、角速度与指令力矩。新鲜有效帧驱动 3D 外骨骼；断流、腿板失效或仿真来源时清空读数并停止姿态更新，断线后每秒重连。浏览器不发送控制命令，不占用串口。
- `site/app.js`、`index.html`、`styles.css` 增加实时与历史记录切换、连接状态和实时数据面板。旧服务缺少 `body` 时，数据来源显示为“未标记”；历史桌面实测记录始终单独标识。
- 新增 Cesium Rigged Figure 简化人体模型，改为半透明并将左右髋骨骼绑定到 3D 外骨骼的角度变化；可在网页显示或隐藏，许可和改动见 `site/assets/HUMAN-LICENSE.md`。当前模型外观尚未达到官网写实程度。
- 更新 `docs/FEATURES.md` 与 `site/README.md` 的行为和限制。验证期间设备服务曾为 `TRIPPED`、`legs_offline=true`、0 Hz，随后恢复至约 178–179 Hz；Chrome 已验证离线显示、3D 加载、历史记录及实时读数。控制进程以 `--body real --profile table` 启动，但旧服务未在状态消息中标记 `body`，网页仍显示“来源未标记”。设备静置，真机动作联动未做现场运动验证。

### 改为本地展示

- 用户最新要求停止公网发布，Cloudflare Pages 项目 `exo-ghost` 已删除。`site/` 保留为本地网页，运行方法见 `site/README.md`。
- 项目规则与全局跨会话规则改为：每次功能更新同步 `site/`、`docs/FEATURES.md`、`CHANGELOG.md`，只在本机验证，不部署公网。

### 产品工作区首版与展示说明同步

- `integration/product-GPT` 的 `9e7996a` 集成运动模式、行走档案、个人记忆和成长徽章；7 个新分支各有独立 worktree，产品端口自动区分，SQLite 数据库默认在各自工作目录。
- 新增产品页面及模块接口。曲线订阅现有控制服务，默认只读、不占用串口；开启产品控制仍需确认实时真机来源及就绪状态。个人偏好保存、版本记录和徽章解锁不会触发设备施力。
- 档案收录从 CSV 与配套 JSONL 确认来源、按内容去重，跳过无效帧及断流间隔；仿真记录不进入产品档案。四枚成长徽章只统计完整真实穿戴运动，排除桌面、急停和静止记录。
- 兼容性：集成版控制状态新增 `body`、`profile`；旧服务仍可显示曲线，但缺少真机来源标记时新产品页面不开放控制。切换控制服务必须先释放串口。原实时控制安全阈值与斜坡未改。
- 将上述行为、数据来源、验证范围和未完成项补入 `docs/FEATURES.md`。展示网页同步四个已实现模块和产品分支代码入口；引路、肌力测量、疲劳/省力结论不作为现成功能宣传。网页不包含私人档案或本机控制入口。
- 验证：134 项 Python 测试、3 项前端控制测试通过；Chrome 检查真机数据只读显示、档案收录及重启保留、桌面记录不计徽章。本地展示页的桌面/移动显示见本次交付记录。

### 展示网页与同步规则

- 新增静态展示网页 `site/`：展示 Ghost 控制链路、三层 Agent、Sim to Real、3D 模型与产品集成分支中的运动模式、行走档案、个人记忆、成长徽章。Cloudflare 曾测试发布，现已撤下。
- 3D 展示使用已确认可用的 Hypershell 官方素材、Tripo 生成模型及真实设备桌面标定回放。网页与本机实时控制服务隔离；无施力入口。
- 新增项目规则 `AGENTS.md`、代码功能索引 `docs/FEATURES.md` 和站点维护说明 `site/README.md`。`.gitignore` 不再忽略项目规则，便于后续工作区合并并由其他会话读取。
- 首次网页检查发现模型加载后预览图未隐藏，已修正；测试发布时内嵌贴图的 `blob:` 地址曾被云端策略拦截，撤站前已修复。Wrangler 缓存目录加入忽略列表。
- 验证：本机静态网页的桌面、移动端显示和 3D 回放。

### 已有功能的分支来源

- `feat/auto-reconnect-GPT`：串口恢复与本机 3D 真机/仿真/回放视图。展示网页复制经确认的模型和桌面回放素材，未改动该分支控制逻辑。
- `integration/product-GPT`：本机产品工作区，含运动模式、行走档案、个人记忆、成长徽章。展示网页仅描述该分支已经实现的范围；主分支尚未合入该产品代码。
