# 更新日志

本日志供后续修改代码时追溯变更、受影响文件与验证结论。每次功能变更都同步更新本日志、`docs/FEATURES.md` 和公开网站 `site/`。

## 2026-09-23

### 串口持续恢复与本地说明同步

- `feat/auto-reconnect-GPT` 在每轮恢复时重新枚举串口，移除原有 40 次重试上限；服务可在设备未插入时启动仪表盘，随后持续等待真实设备。默认来源改为 `real`；仿真和自动回退需分别显式指定 `--body sim`、`--body auto`。
- 修复服务启动时无串口、稍后首次连上后未启动断流监测的问题（`a8d3653`）。新增回归测试覆盖首次连接之后再次断开并更换端口，恢复后的目标力矩为零。
- 串口断流后清除旧策略和保活脉冲，恢复时执行 15 秒零指令等待；仪表盘收到新帧即可恢复绘图，不把缺失值显示为正常零读数。正常关闭时允许接收 `DISABLE` 应答，避免误报关闭失败。
- 补齐 `docs/serial-recovery.md`、`docs/FEATURES.md` 与本地 `site/` 的行为说明，解释腿板全零推断和可选保活脉冲。保活会驱动左腿，防待机效果未验证；不作为已经解决自动关机宣传。
- 验证：126 项 Python 测试通过；桌面真机已检查实时数据、一次自然断流恢复和保活脉冲后归零。尚未完成反复物理拔插/重启压力测试、保活防待机效果及穿戴恢复验证。本次不重启运行中的硬件服务；启动监测修复在下次服务启动时载入。

### 产品工作区首版与公开说明同步

- `integration/product-GPT` 的 `9e7996a` 集成运动模式、行走档案、个人记忆和成长徽章；7 个新分支各有独立 worktree，产品端口自动区分，SQLite 数据库默认在各自工作目录。
- 新增产品页面及模块接口。曲线订阅现有控制服务，默认只读、不占用串口；开启产品控制仍需确认实时真机来源及就绪状态。个人偏好保存、版本记录和徽章解锁不会触发设备施力。
- 档案收录从 CSV 与配套 JSONL 确认来源、按内容去重，跳过无效帧及断流间隔；仿真记录不进入产品档案。四枚成长徽章只统计完整真实穿戴运动，排除桌面、急停和静止记录。
- 兼容性：集成版控制状态新增 `body`、`profile`；旧服务仍可显示曲线，但缺少真机来源标记时新产品页面不开放控制。切换控制服务必须先释放串口。原实时控制安全阈值与斜坡未改。
- 将上述行为、数据来源、验证范围和未完成项补入 `docs/FEATURES.md`。公开网站同步四个已实现模块和产品分支代码入口；引路、肌力测量、疲劳/省力结论不作为现成功能宣传。网页不包含私人档案或本机控制入口。
- 验证：134 项 Python 测试、3 项前端控制测试通过；Chrome 检查真机数据只读显示、档案收录及重启保留、桌面记录不计徽章。公开页面的桌面/移动显示与发布结果见本次交付记录。

### 公开网站与同步规则

- 新增 Cloudflare Pages 静态宣传网站 `site/`：展示 Ghost 控制链路、三层 Agent、Sim to Real、3D 模型与产品集成分支中的运动模式、行走档案、个人记忆、成长徽章。
- 公开 3D 展示使用已确认可用的 Hypershell 官方素材、Tripo 生成模型及真实设备桌面标定回放。所有公开数据均与本机实时控制服务隔离；无远程施力入口。
- 新增项目规则 `AGENTS.md`、代码功能索引 `docs/FEATURES.md` 和站点维护说明 `site/README.md`。`.gitignore` 不再忽略项目规则，便于后续工作区合并并由其他会话读取。
- Cloudflare 首次公网检查发现内嵌贴图的 `blob:` 地址被页面策略拦截，已放行模型贴图并修正加载后预览图的隐藏行为；Wrangler 缓存目录加入忽略列表。
- 验证：静态站点的桌面、移动端显示和 3D 回放；部署后检查公开 URL。部署结果以当次实际记录为准。

### 已有功能的分支来源

- `feat/auto-reconnect-GPT`：串口恢复与本机 3D 真机/仿真/回放视图。公开网站复制经确认的模型和桌面回放素材，未改动该分支控制逻辑。
- `integration/product-GPT`：本机产品工作区，含运动模式、行走档案、个人记忆、成长徽章。公开站点仅描述该分支已经实现的范围；主分支尚未合入该产品代码。
