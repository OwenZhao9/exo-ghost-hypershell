# exo-ghost 项目规则

本文件适用于本仓库所有功能分支和 worktree。涉及外骨骼施力时，先遵守 [安全规则](docs/safety-rules.md)；任何力都必须缓慢加载，不能为了展示效果绕过限幅、静默期或急停。

## 功能更新的同步交付

用户最新要求：今后每次新增、修改或移除功能，都同时维护本地展示网页、功能说明文档与代码更新日志。网页只在本机使用，不发布到 Cloudflare。完成一次功能工作时：

1. 更新 `docs/FEATURES.md` 中对应功能的行为、入口、关键文件、数据来源、约束、已验证范围和未完成项。新增功能先在该文档增加条目；不要把计划写成已实现。
2. 在 `CHANGELOG.md` 的最新日期下记录本次变更、影响范围、迁移或兼容性注意事项，以及验证方法。它是供后来改代码的人阅读的工程日志，不能只写宣传语。
3. 同步更新 `site/` 的相关文案、交互与经授权的素材。内容必须与当前已验证实现一致，区分真机、桌面回放与仿真；未完成或未验证的功能不能伪装成可用。
4. 检查网页素材不包含密钥、私人记录、未经许可的第三方素材或不必要的设备控制入口。3D 页面按用户最新要求只显示“松劲”停止按钮，不显示“急停”按钮；底层急停与安全锁存机制必须保留。运动模式只向现有本机控制服务发送命令，不得直接打开串口或绕过安全档、斜坡、限幅和急停。真机来源、安全档、状态与数据新鲜度未确认时不得开放施力按钮；重连不自动恢复施力。
5. 验证相关测试及网页在桌面和移动端的实际显示。用本机 HTTP 服务打开 `site/` 检查；不要部署到 Cloudflare 或其他公网服务，除非用户以后再次明确要求。

本地运行方法见 [站点维护说明](site/README.md)。功能分支与并行会话应避免覆盖彼此文件；集成后以已合入、已验证的代码为准同步展示内容。

## 代码与硬件边界

- `bridge/` 处理串口、协议与仿真；`control/` 处理安全和策略；`agent/` 处理判断与经验；`runtime/` 处理状态和命令；`dashboard/` 与 `product/` 负责本机界面；`site/` 提供本地 3D 展示及上述受限运动模式入口。
- 新功能优先按职责拆分文件，保持纯计算与 IO 分离。串口读线程只能做常数时间工作。
- 真机只有一个控制进程可以独占串口；网站、产品只读模式和测试不得自行争抢串口。
- 修改安全行为后运行 `uv run pytest tests/ -q`；不要为了通过测试重录安全基准。测试结论只覆盖实际运行过的范围。
- 涉及产品工作区时阅读 `docs/product-status.md` 与 `docs/product-branches.md`（若当前分支包含这些文件）。
